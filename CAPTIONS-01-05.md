# Caption Facebook/Threads — Series "Code4Life · OOP Beginner: Class & Object" (bài 01-05)

---

**Bài 01 — Rectangle (Hình chữ nhật)**
Một class chỉ là cái khuôn — object mới là thứ được đúc ra từ khuôn đó.
Bài đầu tiên của series OOP: gom dữ liệu (`width`, `height`) và hành vi (`area`, `perimeter`) vào chung một chỗ. Nghe đơn giản, nhưng đây chính là viên gạch đầu tiên của mọi hệ thống phần mềm lớn bạn từng dùng.
#Code4Life #OOP #LearnToCode #LapTrinh

---

**Bài 02 — StudentRecord (Hồ sơ học sinh)**
Một học sinh có tên, tuổi, điểm — và một quy tắc: điểm >= 5 là đậu.
Bài học hôm nay: quy tắc đó nên nằm ở ĐÂU? Rải rác khắp nơi hay gói gọn trong 1 hàm để cả class dùng chung? Đây là thói quen phân biệt code "chạy được" và code "dễ sửa".
#Code4Life #OOP #CleanCode

---

**Bài 03 — Circle (Hình tròn)**
PI có bao giờ đổi giữa các hình tròn không? Không — vậy tại sao phải bắt mỗi object tự giữ một bản riêng?
Bài này giới thiệu `static`: dữ liệu dùng CHUNG cho cả class, khác với dữ liệu riêng của từng object. Nhầm hai khái niệm này là lỗi kinh điển của người mới học OOP.
#Code4Life #OOP #JavaCoBan

---

**Bài 04 — Counter (Bộ đếm)**
Khác với 3 bài trước — object hôm nay có trí nhớ. Gọi `increment()` vài lần, giá trị bên trong nó thực sự thay đổi.
Đây là ý tưởng lõi đứng sau giỏ hàng, phiên đăng nhập, kết nối mạng — mọi thứ có "trạng thái" trong phần mềm đều bắt đầu từ đây.
#Code4Life #OOP #State

---

**Bài 05 — Person (Object chào object)**
Đỉnh cao của tầng Beginner: một object không chỉ tự xử lý dữ liệu của mình, mà còn "nói chuyện" được với object khác — `an.greet(binh)`.
Đây chính là bước đệm trực tiếp để bước sang tầng tiếp theo: đóng gói, kế thừa, đa hình. Chốt tầng Beginner — hẹn gặp lại ở Student với 4 trụ cột OOP.
#Code4Life #OOP #InterviewPrep
